public class Uplow {
	String str= 'DeXtarA';
    public void dex()
    {	String str1= str.toLowerCase();
     	String str2='dextarA';
        System.debug(str.toLowerCase());
         System.debug(str.toUpperCase());
        System.debug(str2.compareTo(str1));
     	System.debug(str2.equals(str1));
    }
}